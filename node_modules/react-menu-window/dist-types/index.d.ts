import * as React from "react";
declare type ContentPosition = {
    top: number;
    left: number;
};
export declare type MenuWindowProps = {
    children: React.ReactElement<any>;
    hideOn: string;
    getPosition: (e: React.MouseEvent) => ContentPosition;
    render: (e: React.MouseEvent, props: {
        close: () => void;
    }) => React.ReactElement<any>;
    shouldOpen: () => boolean;
    onOpen: (e: React.MouseEvent) => void;
    onClose: (e: React.MouseEvent) => void;
    viewportOffset: number;
};
export declare function MenuWindow({ children, getPosition, hideOn, render, shouldOpen, onOpen, onClose, viewportOffset, }: MenuWindowProps): React.ReactElement;
export {};
