import * as React from "react";
import { DialogOptions } from "../Dialog";
export declare function usePortal(dialogRef: React.RefObject<HTMLElement>, options: DialogOptions): (children: React.ReactNode) => React.ReactNode;
